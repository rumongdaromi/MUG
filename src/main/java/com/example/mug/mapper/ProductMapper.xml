<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mug.mapper.ProductMapper">

    <select id="findAll" resultType="com.example.demo.domain.Product">
        SELECT * FROM Product
    </select>

    <select id="findById" parameterType="Long" resultType="com.example.demo.domain.Product">
        SELECT * FROM Product WHERE ProductID = #{productId}
    </select>

    <insert id="insert" parameterType="com.example.demo.domain.Product">
        INSERT INTO Product (ProductID, 상품코드, 이름, 카테고리, 성별, 가격, 사이즈, 옵션1, 옵션2)
        VALUES (#{productId}, #{productCode}, #{name}, #{category}, #{gender}, #{price}, #{size}, #{option1}, #{option2})
    </insert>

    <update id="update" parameterType="com.example.demo.domain.Product">
        UPDATE Product
        SET 상품코드 = #{productCode}, 이름 = #{name}, 카테고리 = #{category}, 성별 = #{gender}, 가격 = #{price}, 사이즈 = #{size}, 옵션1 = #{option1}, 옵션2 = #{option2}
        WHERE ProductID = #{productId}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM Product WHERE ProductID = #{productId}
    </delete>

</mapper>
