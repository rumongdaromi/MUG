<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mug.mapper.OrderMapper">

    <!-- 주문 객체를 위한 결과 매핑 정의 -->
    <resultMap id="orderResultMap" type="com.example.mug.entity.Order">
        <id column="order_id" property="orderId"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="order_date" property="orderDate"/>
    </resultMap>

    <!-- 주문 추가 -->
    <insert id="insertOrder" parameterType="com.example.mug.entity.Order">
        INSERT INTO orders (user_id, product_id, quantity, order_date)
        VALUES (#{userId}, #{name},#{productId}, #{quantity}, #{orderDate})
    </insert>

    <!-- 사용자 ID로 주문 조회 -->
    <select id="findOrdersByUserId" resultMap="orderResultMap">
        SELECT order_id, user_id, product_id, quantity, order_date
        FROM orders
        WHERE user_id = #{userId}
    </select>

    <!-- 주문 ID로 주문 조회 -->
    <select id="findOrderByOrderId" resultMap="orderResultMap">
        SELECT order_id, user_id, product_id, quantity, order_date
        FROM orders
        WHERE order_id = #{orderId}
    </select>
</mapper>
